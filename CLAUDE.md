# CLAUDE.md

이 파일은 Claude Code(claude.ai/code)가 이 저장소에서 작업할 때 따라야 할 **큰 원칙**만 담는다.
구현 세부 사항(아키텍처 규칙, View 작성법, 네트워킹, 커밋 컨벤션 등)은 프로젝트가 커지면서 `.claude/skills/`에 필요한 시점에 추가해 참조한다. 아직 스킬이 없다면 이 파일의 원칙을 기본값으로 따른다.

프로젝트: **Anywhere (아무데나)** — 지역 상생을 위한 랜덤 여행 & 도장 깨기 iOS 앱. 사용자가 미리 목적지를 정하지 않고 랜덤/추천으로 로컬 장소를 발견하고, 방문을 "도장 깨기"처럼 기록·수집하며 지역 소상공인·명소와의 상생을 유도하는 서비스. SwiftUI + Tuist + Clean/Micro-Feature Architecture.

---

## Workflow Orchestration

### 1. Plan Node Default
- 사소하지 않은 작업(3단계 이상이거나 아키텍처 결정을 포함하는 작업)에는 **항상 plan 모드로 진입**한다.
- 작업이 어긋나기 시작하면 **즉시 멈추고 재계획**한다 — 그냥 밀어붙이지 않는다.
- 빌드뿐 아니라 **검증 단계에도 plan 모드를 활용**한다.
- 모호함을 줄이기 위해 **상세 스펙을 먼저 글로 작성**한다.

### 2. Subagent Strategy
- 위임은 **크고 독립적이며 병렬 실행이 가능한 작업에만** 한다 — 여러 파일·디렉터리를 넓게 훑는 탐색, 서로 겹치지 않는 리서치 갈래가 그 예다.
- **자기 결과 확인용 위임은 금지.** 방금 내가 쓴 것을 다른 에이전트에게 "봐 달라"고 넘기는 건 위임이 아니라 미루기다. 다시 봐야 하면 내가 다시 읽는다.
- **동시 스폰 수는 낮게 유지한다.** 갈래 수를 늘리는 대신 각 갈래를 좁게 정의한다. 서브에이전트 1개당 **하나의 방향**만.

### 3. Self-Improvement Loop
- 사용자에게 **교정받을 때마다** `tasks/lessons.md`에 해당 패턴을 기록한다.
- 같은 실수를 막을 **자기 규칙**을 직접 작성한다.
- 실수율이 떨어질 때까지 이 기록을 **집요하게 반복 개선**한다.
- 세션 시작 시 해당 프로젝트의 lessons를 **먼저 검토**한다.

### 4. Verification Before Done
- 동작을 **증명하지 않고는** 작업을 완료로 표시하지 않는다.
- **컴파일/빌드 통과는 검증이 아니다.** "검증"이라는 단어는 런타임 동작 확인(앱 실행, 로그 관찰, 동작 시연)에만 쓴다. 빌드만 돌렸으면 "컴파일 통과"라고만 말한다.
- 자동으로 확인할 수 없는 것(시각 렌더링 등)은 **"확인 불가 — 육안 필요"**로 정직하게 말한다. 됐다고 단정하지 않는다.
- **검증 강도는 변경 크기에 맞춘다.** 대부분은 내가 직접 실행·로그·시연으로 증명하면 끝난다. 별도 리뷰 lane(`code-reviewer`/`verifier`)은 **리스크가 크거나 스스로 판단이 갈릴 때만** 붙인다 — 모든 비사소 작업에 일괄로 강제하지 않는다.
- "**staff 엔지니어가 이 코드를 승인할까?**"를 스스로 묻고, 필요하면 main 브랜치와 변경 동작을 **diff로 비교**한다.
- 테스트 실행, 로그 확인, 동작 시연으로 정확성을 입증한다.

### 5. Demand Elegance (Balanced)
- 사소하지 않은 변경에는 **"더 우아한 방법이 있는가?"**를 멈춰서 묻는다.
- 수정이 hacky하게 느껴지면 — "**지금 알게 된 모든 것을 종합해 우아한 해법으로 다시 구현하라**"고 자신에게 요구한다.
- 단순하고 명확한 수정에는 이 단계를 **건너뛴다** — 과한 엔지니어링 금지.
- 결과를 제시하기 전에 **자기 결과물에 도전**한다.

### 6. Autonomous Bug Fixing
- 버그 리포트를 받으면 **그냥 고친다**. 손잡고 끌어 달라고 하지 않는다.
- 로그, 에러, 실패한 테스트를 가리키고 — 그걸 해결한다.
- 사용자에게 **컨텍스트 스위칭을 강요하지 않는다**.
- 실패한 CI 테스트는 따로 지시받지 않아도 가서 고친다.

### 7. Apply, Don't Suggest
- 해결 방법을 찾았으면 **그 자리에서 바로 적용**한다. "이렇게 바꾸면 됩니다" 같은 안내로 끝내지 않는다.
- 사용자에게 **수정을 시키지 않는다** — 코드 스니펫만 던지고 떠넘기는 패턴 금지.
- 단순 수정(파라미터 추가, 플래그 토글, 호출부 한 줄 교체 등)은 **무조건 직접 Edit**으로 적용한다.
- 영향 범위가 크거나 destructive한 변경(여러 모듈, 공용 컴포넌트 시그니처 변경, 삭제)은 "이렇게 적용할까요?"로 **명확한 risk를 적시해 한 번만 확인**한다. "수정할까요?" 같이 떠넘기는 모호한 질문 금지.
- 변경 후에는 영향 받는 모든 호출부까지 **연쇄적으로 적용**한 뒤 보고한다.

---

## Task Management

1. **Plan First**: 작업 계획을 `tasks/todo.md`에 체크 가능한 항목으로 적는다.
2. **Verify Plan**: 구현 시작 전에 계획을 확인받는다.
3. **Track Progress**: 진행하며 항목을 완료 처리한다.
4. **Explain Changes**: 각 단계에서 변경 요약을 high-level로 설명한다.
5. **Document Results**: `tasks/todo.md`에 리뷰 섹션을 덧붙인다.
6. **Capture Lessons**: 교정받은 후에는 `tasks/lessons.md`를 갱신한다.

---

## Core Principles

- **Simplicity First**: 모든 변경을 가능한 한 단순하게. 코드를 최소한으로만 건드린다.
- **No Laziness**: 근본 원인을 찾는다. 임시방편 금지. 시니어 개발자의 기준을 적용한다.
- **Minimal Impact**: 변경은 필요한 곳에만 닿는다. 새로운 버그를 만들지 않는다.
- **No Unsolicited Tests**: 테스트 코드는 사용자가 명시적으로 요청("테스트 써줘", "tdd")할 때만 작성한다. 기능 구현/수정 시 테스트를 선제 작성·추가하지 않는다. 내 변경으로 기존 테스트 컴파일이 깨지면 최소 수정으로만 맞춘다. 검증은 빌드 + 런타임 동작 확인 중심.

---

## 프로젝트 구조 (Tuist Workspace)

Micro-Feature 모듈 구성 (`Workspace.swift` 기준):

| 모듈 | 역할 |
|------|------|
| `AnywhereApp` | 앱 엔트리포인트, 각 Feature/Core 모듈 조립 |
| `Auth` | 로그인/인증 (SDK 연동 등) |
| `Domain` | UseCase, Entity, Repository 프로토콜 — 순수 비즈니스 로직 |
| `Data` | Repository 구현체, DTO, Mapper, API 연동 |
| `Presentation` | SwiftUI View, ModelData, 화면 단위 Feature |
| `UIComponents` | 재사용 가능한 디자인 시스템 컴포넌트 |
| `Core` | 공통 유틸리티, 확장, 기반 타입 |
| `DIContainer` | 의존성 주입 조립 |

의존성 방향은 Clean Architecture 원칙(View → Presentation → Domain ← Data, Core/UIComponents는 하위 공용 계층)을 따른다. 새 모듈이나 Feature 추가 시 이 표를 갱신한다.

---

## 프로젝트 스킬 & 슬래시 커맨드 (구현 세부 사항)

아직 이 저장소에는 `.claude/skills/`나 프로젝트 전용 슬래시 커맨드가 없다. 반복되는 구현 규칙(아키텍처 검증, 빌드, 커밋 컨벤션, 모듈 추가 절차 등)이 생기면 AFin 프로젝트의 사례처럼 스킬로 분리해 이 표에 등록한다.

| 스킬/커맨드 | 다룰 때 사용 |
|------|-------------|
| _(아직 없음)_ | 필요해지면 `.claude/skills/<name>/SKILL.md`로 추가하고 여기 등록 |

---
